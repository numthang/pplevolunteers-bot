// /org/* — เลิกใช้ OrgShell แล้ว (switcher รอง + เมนูรอง หน้าหลัก/ตั้งค่า ซ้ำกับ main Nav)
// switch/สร้าง/ออก อยู่ที่ OrgSwitcherMenu บน main Nav · render children ตรงๆ เต็มความกว้าง (root main max-w-5xl)
export default function OrgLayout({ children }) {
  return children
}
