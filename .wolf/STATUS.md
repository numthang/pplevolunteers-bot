# STATUS — 2026-09-08

## ➜ เควสต์ปัจจุบัน: สาย faceless ขั้นที่ 1 **ขึ้น prod ครบแล้ว** — ตัวชี้ขาดถัดไปคือ "บท" ไม่ใช่เครื่องมือ

### ✅ ตัดสินใจจบแล้ว: ไปทาง ค. (เขียนเครื่องอัดเอง) — ทาง ง. (แอปสำเร็จรูป) ตกไป
ผลทดสอบ `/dev/camtest` **ผ่านครบ 3 ข้อ ทุกเครื่อง (ไอโฟน Safari · แอนดรอยด์ Chrome · คอม)**
ภาพชัด · เสียงชัด · **"ความยาวที่อ่านได้" ขึ้นเป็นตัวเลข** → 2 ใน 3 ช่องโหว่เดิมตกไปเอง
(คุณภาพวิดีโอ + ไฟล์ไม่มีข้อมูลความยาว) เหลือแค่ข้อ "อัปทับคลิปเดิม" ซึ่ง**แก้แล้ว**

### 🔨 ทำอะไรไปแล้ว — commit `acbc3f4` (merge `wip/browser-recorder` เข้า master)
- **`?replace=1`** ที่ `web/app/api/posts/[id]/media/video/route.js` — อัดทับในคำขอเดียว
  ลำดับคือ **เขียนไฟล์ใหม่สำเร็จก่อน → `replaceVideoFile()` สลับแถว → ค่อยลบไฟล์เก่า**
  ⛔ ห้ามกลับไปให้ client ลบคลิปเก่าก่อนแล้วค่อยอัป — เน็ตมือถือหลุด = เสียทั้งสองทาง
- ทางอัปทับ**ข้ามด่านนับสื่อทั้งสองด่าน**โดยตั้งใจ (จำนวนแถวเท่าเดิม)
- แถวเดิมหายระหว่างอัป → ตกไป `addMedia()` · ลบไฟล์เก่าเช็ค `pathStillUsed()` ก่อน
- `web/db/posts/media.js` เพิ่ม **`findVideoOfPost()`** — เซิร์ฟเวอร์หาแถวคลิปเอง ไม่รับ id จาก client
- คำแปล th/en ครบ 12 key ที่ `posts.script.*`
- **verify แล้ว:** build ผ่าน · `mobileAudit /posts/1047/script` ผ่าน · ทดสอบ replace ระดับ DB 6/6

### ✅ ขึ้น prod แล้ว 2026-09-08 (`950c953`) — ขั้นที่ 1 ของสาย faceless ถือว่าจบ
- **bug-491** เลื่อนบทจนสุด ≠ จบเทค (`779ab7c`) · ⛔ **ห้ามผูก "เลื่อนจบ" กับ "จบเทค" อีก**
  ใช้ `inTakeRef` · หางว่างท้ายบท 20vh → 32vh (กล่องสูง 38vh)
- **ปุ่มเร่ง/ชะลอระหว่างอัด** (`950c953`) — ของเดิมพอเริ่มอัดเหลือแค่ปุ่มหยุด มือถือไม่มีคีย์บอร์ด
  ⛔ ห้ามใส่ปุ่มปรับ**ขนาดตัวอักษร**ระหว่างอัด — บทรีโฟลว์ กระโดดหลุดบรรทัดกลางเทค
- **`?replace=1`** อัดทับคลิปเดิมในคำขอเดียว (`acbc3f4`) · ⛔ ห้ามให้ client ลบของเก่าก่อนอัป

### 🔎 ผลสัมภาษณ์ user หลังใช้จริง (2026-09-08) — **ตัวกำหนดงานถัดไป**
| ถาม | ตอบ |
|---|---|
| ตอนไหนกินแรงสุด | **เขียน/แก้บทก่อนอัด** (ไม่ใช่การอัด ไม่ใช่ตัดต่อ) |
| ตอนอ่านบทสะดุดอะไร | ความเร็วเลื่อนไม่พอดี (**แก้แล้ว**) · **เนื้อบทไม่เข้าปาก** |
| เอาคลิปขึ้นเพจจริงหรือยัง | **ยังไม่ได้ลอง** — ปลายท่อยังไม่เคยพิสูจน์ |
| ต้องมีหน้าอยู่ในคลิปไหม | **บางคลิปออก บางคลิปไม่ออก** — ระบบตอนนี้ทำโหมดไม่ออกหน้าไม่ได้เลย |

➜ **คอขวดคือ "บท" ไม่ใช่เครื่องมือ** ตรงกับกติกาในแผนเองที่ว่า *"ถ้ายังติดขัด ปัญหาอยู่ที่บทพูด
ให้ไปปรับก่อนจะขึ้นขั้นที่ 2"* → **ขั้น 2 (ถอดเสียง/ซับ) ยังไม่ใช่ตอนนี้ อย่าเพิ่งเริ่ม**

### ⏭️ ทำอะไรต่อ — เรียงตามที่ตกลงกับ user
1. **[user ต้องลองเอง] เอาคลิปที่อัดไปโพสต์ขึ้นเพจจริง 1 คลิป** — เสี่ยงสุดและถูกสุด
   ไฟล์จากเบราว์เซอร์ต่างจากไฟล์จากแอปกล้อง · ถ้าเพจไม่รับ งานที่เหลือเสียเปล่าหมด
2. **แก้ "บทไม่เข้าปาก"** — ปรับ `head` ของ slot `posts.script` ใน `config/aiPrompts.js`
   + เพิ่มปุ่ม "เกลาย่อหน้านี้ให้พูดง่ายขึ้น" กดทีละย่อหน้าในหน้าอ่าน (ยังไม่ได้ออกแบบ)
3. **โหมดไม่ออกหน้า (faceless จริงๆ)** — เสียง + ภาพนิ่งจากคลัง · ของใหม่ทั้งก้อน **ต้องคุยสเปกก่อน**
   (ชื่องาน `faceless` แปลว่า user อาจเล็งอันนี้ไว้ตั้งแต่ต้น — ถามให้ชัดก่อนออกแบบ)
4. **ลบ `web/app/dev/camtest/page.js`** — หน้าทดสอบชั่วคราว หมดหน้าที่แล้ว

### 🧪 เครื่องมือ: `scripts/dev/recorderRepro.mjs`
Chrome headless + **กล้อง/ไมค์ปลอม** บน localhost (secure context เหมือน https) → กดอัดจริงแล้ววัดผลได้
จับ bug-491 + ยืนยันปุ่มความเร็วมาแล้ว · ต้องมี dev server: `cd web && NEXT_DIST_DIR=.next-audit PORT=3100 npx next dev -p 3100`
⛔ เขียนทับบทพูดของโพสต์ที่ระบุ (default `POST=793`) — ใช้กับโพสต์ทดสอบที่เป็น draft เท่านั้น

### ⚠️ หนี้ที่เพิ่งเกิดจากฟีเจอร์นี้ (จดใน `md/PENDING.md` แล้ว)
คลิปที่อัดในเว็บไม่มี `source_url` → `services/postsRetention.js:46` **ไม่มีวันเก็บกวาดให้**
ดิสก์เว็บโตทางเดียว · ยังไม่เจ็บ (`storage/posts` 1.6 GB) แต่เร่งคิวงาน "ย้าย storage ออกจากดิสก์เว็บ"

## ⛔ ข้อตกลงสายงานคลิป (ห้ามเปลี่ยนเอง)
- ห้ามให้ AI แต่งเนื้อหาใหม่ในบทพูด — แปลง*รูปแบบ*จาก body เท่านั้น
- **ห้ามเสนอ TTS / avatar / เจนภาพประกอบ** · ภาพประกอบต้องมาจากคลังรูปจริงของ user
- user **ไม่เข้าใจศัพท์เทคนิคเรื่องคลิป** → อธิบายด้วยภาษาคน
- ข้อจำกัดจริง: อัด**นอกบ้าน มือถือเครื่องเดียว** → ทางที่ต้องมีจอที่สอง (teleprompter กระจก/สองเครื่อง) ตกไปหมด
- เกณฑ์ขึ้นขั้นถัดไป: ทำคลิปจริง 3-5 คลิปก่อน แล้วดูว่าอะไรน่ารำคาญที่สุด — ไม่ใช่เดาล่วงหน้า

---

## ✅ เสร็จก่อนหน้า
- **บทพูด default ผู้พูดเป็นผู้ชาย** (`e841307`) — กฎ "ผม/ครับ" อยู่ใน `head` ของ `posts.script` (`config/aiPrompts.js`)
  แต่ละ org แก้ทับเองได้ที่ `org_ai_prompts` ไม่ต้องแตะโค้ด
- **KANBAN ชั้น teamspace — ขึ้น prod แล้ว** · เหลือ `ALTER TABLE kanban_boards ALTER COLUMN teamspace_id SET NOT NULL;`
  (เขียนเป็น migration ไว้แล้ว **ยังไม่ได้รันบน prod**)
  ⛔ "teamspace" ทับศัพท์ · custom field ผูกกระดาน ไม่ใช่ teamspace · กันสิทธิ์จริงต้องแก้ `web/db/kanban/scopeSql.js` 3 จุดพร้อมกัน

## ➜ ค้างไว้ (ไม่ด่วน)
1. Posts โหมด AI "ร่างตามคำแนะนำ" (`5c73c8b`) — user ยังไม่กดทดสอบ
2. แท็บ "นำเข้าแล้ว" ยังไม่มีลิงก์ไปการ์ด KB-xxx · ยังไม่มีปุ่ม "ยกเลิกการนำเข้า"
3. `caseUploads.getCaseUploadDir()` / `cropDocument.getUploadPath()` มีบั๊ก cwd แบบที่แก้ไปแล้ว
4. หนี้ i18n ทั้งโซน posts (`PostEditor.jsx`) — ดู `md/PENDING.md`

## ⚠️ รู้ไว้
- **กล้องในเบราว์เซอร์ (`getUserMedia`) ทำงานเฉพาะ https** — ทดสอบผ่าน `http://<ip>:3000` ไม่ได้
- **หลังรีบูตเครื่อง dev → ssh เข้า prod พัง** (`agent refused operation`) เพราะ gnome-keyring ล็อก
  ให้ user รัน `ssh tee@202.183.141.78 true` ในเทอร์มินัลตัวเองครั้งเดียว
- **prod ยังมี GitHub token ฝังใน remote url** — วันไหนออก token ใหม่ต้องไปแก้ที่ prod ด้วย
- dev server ของ user รันที่พอร์ต 3000 — จะ build ต้องตั้ง `NEXT_DIST_DIR` แยก (ครั้งนี้ใช้ `.next-verify` แล้วลบทิ้ง)
- ล็อกอินทดสอบ local: `mobileAudit.mjs` ทำเองได้ (insert token ลง `org_login_tokens`) แต่ต้องมี dev server รันอยู่ก่อน

## 🧷 ของค้างในเครื่อง dev ที่ไม่ใช่ของ Claude (2026-09-08)
`utils/quoteStyleKeys.js` · `web/lib/quoteStyles.js` · `web/locales/th.json` มีการแก้คำ
**"การ์ดคำคม" → "การ์ดโควต"** ค้างอยู่ — **เป็นงานที่ user แก้เอง ห้าม commit ให้ ห้ามรื้อ**
