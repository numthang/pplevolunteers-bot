# STATUS — 2026-09-15

## ✅ เพิ่งเสร็จ: นำเข้าเลขบัญชีสมาชิกขึ้น prod แล้ว (`293a888` + `findMember` · deploy แล้ว)

**ผลบน prod:** เขียน 63 คน / 136 แถว · ก่อนรัน 42 คน → **หลังรัน 64 คนมีข้อมูลรับเงินใน org 1**
ไฟล์ต้นทาง `/tmp/backfill-bank-2569.xlsx` บนเครื่อง prod (ชีต `Sheet4` 68 แถว · ห้ามเข้า git)
local (`platfor`) 62 คน — ตัวเลขต่างกันเพราะ local เป็นก๊อปปี้เก่า ไม่มี Kuu/Sutlaa

**⚠️ ที่ต้องรู้:** 41 ใน 63 คนนั้น**มีข้อมูลรับเงินอยู่ก่อนแล้วบน prod** (คนกรอกเองผ่าน
/profile/settings) → ถูกทับด้วยค่าจากไฟล์ · ไม่ได้ diff ก่อนทับ และไม่มี history ให้ย้อน
ถ้าจะทำอีกรอบ **ต้องเช็กก่อนว่าแถวเดิมต่างจากไฟล์ตรงไหน**

**ค้าง 5 คน:** Namfon (ไฟล์เขียน `@namfon05__52871` แต่ prod มีแค่ **`@namfon05_`** id 17996
discord 1528301547891593306 — รอ user ยืนยันว่าคนเดียวกันไหม ใน org มี namfon 3 คน) ·
Tor P' / กาจชัย (มีเลขบัญชีแต่ไม่มี username) · Aommie / Phon (ไม่มีเลขบัญชี)

**deploy รอบนี้ได้อะไรไปด้วย:** `3a5ab04` ลิงก์โพสต์ในตะกร้าสื่ออ่าน `web_base_url` จาก
guild_config + เลิกใช้ masked link (ใน content ของ Discord กดไม่ได้) · bump v2.27.1
⚠️ `web/lib/officeBinaries.js` ยัง untracked — ไม่มีใครเรียก ยังไม่ commit

**เครื่องมือใหม่:** `scripts/finance/findMember.mjs` — อ่านอย่างเดียว หาสมาชิกจาก
username/discord_id (ใช้เวลา backfill ขึ้น "ไม่พบผู้ใช้") · bug-522 จดแล้ว

---

## (เดิม) STATUS — 2026-09-13

## ✅ เพิ่งเสร็จ: งาน Cases 4 ข้อจาก md/TEAM/TEE.md (`26f0c3a` · ยังไม่ push ยังไม่ deploy)

1. **แนบไฟล์เองในหน้าจัดการเคส** — `POST/DELETE /api/case/[ref]/attachments` (ใหม่) · เพดาน 10 ไฟล์
   ต่อเคสนับจากของที่มีอยู่จริง · เพิ่ม `application/pdf` เข้า allowlist (`lib/caseUploads.js` —
   กระทบฟอร์มสาธารณะด้วย ตั้งใจ) · ลบแล้ว unlink ไฟล์ทันที · GET ใส่ `nosniff` + inline เฉพาะรูป
2. **prompt หนังสือร้องเรียนคมขึ้น** — `config/aiPrompts.js` (`case.letter_draft`) + route ส่ง context
   ครบ (เลขเรื่อง/วันที่รับเรื่อง/ผู้ร้อง/**รายการไฟล์แนบจริง**) · เลิก haiku+`task:'light'`
   ⚠️ **ถ้า org เคยแก้ prompt ทับในหลังบ้าน (`org_ai_prompts`) ค่าในโค้ดจะไม่มีผล** — ต้องกด
   "คืนค่าเดิม" ที่หน้า AI ก่อน
3. **แก้บั๊ก** textarea ในโมดัลร่างหนังสือไม่ยืดหลัง พรีวิว→ย้อนกลับ (bug-518)
4. **การ์ด /cases บนมือถือ** — สถานะ+ถังขยะบนขวา · หัวข้อบรรทัดใหม่ · ตัดเลขเคส/จำนวนไฟล์แนบ ·
   รายละเอียด 2 บรรทัด · ผู้รับผิดชอบ+progress เต็มความกว้างใต้รูป

**verify:** test 538 ผ่าน · build ผ่าน · mobileAudit `/cases` ผ่าน · ยิง API จริงครบทุกเคส
(อัปโหลด/mime ไม่รองรับ/เพดาน/headers/ลบแล้วไฟล์หายจากดิสก์) · ร่างหนังสือจริง 2 รอบ
**ยังไม่ได้ให้ user กดจริงในเบราว์เซอร์** — ปุ่มแนบ/ลบไฟล์ต้องให้ user ลองเอง
**หมายเหตุ:** `/cases/[ref]` mobileAudit ยัง exit 1 ที่รูป `object-cover` (C) — **ของเดิม ไม่ใช่ของใหม่**
(ยืนยันด้วยการ stash ไฟล์ที่แก้แล้วรันซ้ำ ได้ผลเท่ากัน)

---

## (เดิม) STATUS — 2026-09-10

## ✅ เพิ่งเสร็จ: ตัวกรอง "ประเมินสมาชิก" หน้า /calling/assignments (`70d239b` · ยังไม่ push)

dropdown 5 ระดับในแถบกรอง ใช้ได้ทั้งแท็บสมาชิกและผู้ติดต่อ (flag อยู่ `calling_member_tiers`
ร่วมกัน) · sync ลง URL `?flag=` · รีเซ็ตตอนสลับแท็บ · แตะ 8 ไฟล์ตามแผนเดิมเป๊ะ

**กับดักค่าเก่าแก้แล้ว:** เพิ่ม `flagQueryValues()` ใน `web/lib/callingFlags.js` คืนอาร์เรย์
(`'good'` → `['good','green']`) แล้ว query ใช้ `t.flag = ANY($n::text[])` ทั้ง members/contacts
⛔ ห้ามเขียน `t.flag = 'good'` ตรงๆ ที่ไหนอีก — แถวค่าเก่าจะหายเงียบ

**verify แล้ว:** query จริงบน dev ทั้ง 2 ฝั่ง (flag=good ดึงแถวที่เก็บ `'green'` ติด · ค่ามั่ว = ไม่กรอง)
· e2e เปิดหน้าจริงผ่าน CDP เลือก "ดี" แล้ว URL เป็น `?flag=good`
· mobile audit: ยัง 9px เท่าเดิม (ของเดิมทั้งโซน ไม่ใช่ของใหม่)
**ยังไม่ได้ให้ user กดจริง** — ของกดได้ต้องให้ user ลอง

## ➜ เควสต์ถัดไป (ยังไม่เคาะ เลือกเอง 1 อย่าง)
1. **query ผู้ติดต่อคืนแถวซ้ำ** — `web/db/calling/contacts.js` `GROUP BY` มี `l.called_at, l.status, l.note`
   → คนที่ถูกโทร 3 ครั้งโผล่ 3 แถว แถวละ `total_calls = 1` (ท่าที่ถูกคือ LATERAL แบบฝั่ง members)
   ⚠️ ถ้าแก้ ต้องขยับตัวกรอง flag/starred/sig ที่อยู่ใน HAVING ตามไปด้วย
2. **mobile audit ยัง exit 1** ที่ 9px — `app/calling/layout.js:10` `-mx-3 sm:-mx-4` ล้นทั้งโซน `/calling`
3. คิวสาย faceless (ดูท้ายไฟล์) — ข้อ 1 ต้องให้ user ลองเอง

### ✅ เสร็จแล้วรอบนี้ — ขึ้น prod แล้วบางส่วน
**ขึ้น prod แล้ว (deploy 2026-09-10):** `a25ace1` race condition · `6459fca` index · `5a4d97c` ตัวกรองดาว/สัญญาณ
วัดหลัง deploy: `getMembersInCampaign` **106ms** (เดิม 2,646ms) · index ทั้ง 2 ตัวยืนยันว่ามีจริงใน pg_indexes

**ชุดเม็ดประเมิน 8 commit (`54d0937`…`e70c640`) — user deploy เองแล้ว 2026-09-10**

**⚠️ ค้างบนเครื่อง dev ตอนนี้: `70d239b` ตัวกรองประเมินสมาชิก** (UI + query ล้วน ไม่มี migration
→ deploy ด้วย pull + build + restart ตามปกติ)

### 🎨 ที่เคาะไปแล้วเรื่องเม็ดสี — อย่ารื้อ
- **สี = hex ของ emoji วงกลม (Twemoji)** 🟢`#78b159` 🟡`#fdcb58` เทา`#99aab5` 🟠`#f4900c` 🔴`#dd2e44`
  ⛔ อย่าเปลี่ยนกลับไปโทนเข้มของ `STATUS_ICONS` (`#1a5e2d`/`#a32d2d`) — user ทักว่า "ไม่ค่อยสว่าง"
- **ไม่ได้เลือก = จาง `opacity-25` · เลือกแล้ว = สีเต็ม** ⛔ ห้ามใส่วงแหวน/ติ๊กขาว/ป้ายข้อความกลับมา (ลองแล้ว ให้ถอด)
- **ช่องไฟ `gap-1`** — เคยบีบเป็น `gap-0` แล้ว user ให้ย้อนกลับ
- **เม็ดในแถว 10px เล็กกว่าใน modal 16px** — เสนอให้เท่ากับดาว (14px) แล้ว **user บอกว่าไม่ต้องแก้**

### 🔜 ค้างไว้ ยังไม่แตะ (user ยังไม่เคาะ)
- **mobile audit ยัง exit 1** ที่ 9px — `app/calling/layout.js:10` `-mx-3 sm:-mx-4` ล้นเท่ากันทั้งก่อน/หลัง
  เป็นของทั้งโซน `/calling` (โมดัลก็ล้นตามเพราะหน้าถูกถ่างเป็น 385px มาก่อนแล้ว)
- **query ผู้ติดต่อคืนแถวซ้ำ** — `web/db/calling/contacts.js` `GROUP BY` มี `l.called_at, l.status, l.note`
  → คนที่ถูกโทร 3 ครั้งโผล่ 3 แถว แถวละ `total_calls = 1` (ท่าที่ถูกคือ LATERAL แบบฝั่ง members)

---

## ⏸ เควสต์ค้าง (รอ user อัดจริง): อัดแล้วต้อง "เหมือนมองกล้อง" ไม่ใช่เหมือนอ่านบท

### 🚀 ขึ้น prod แล้ว (`23ecaad` + `cfd6370`) — **รอ user อัดจริงแล้วดูคลิปตัวเอง**
อาการที่ user รายงาน: "ลูกตาผมมองสคริปต์ ไม่ได้มองกล้อง" (เห็นชัดในคลิปที่อัดออกมา)

**หลักที่ใช้ตัดสิน (จำไว้ใช้ต่อ):** จุดชัดกลางจอตา (fovea) กว้างราว **2°** —
ตัวหนังสือที่อยู่ในกรอบนั้นอ่านออกทั้งที่สายตายังจับอยู่ที่เลนส์ เกินกว่านั้นต้องขยับลูกตา = คนดูเห็น
แปลงเป็นความสูงบนจอตามระยะที่ตั้งมือถือ: **40 ซม. ≈ 1.4 ซม. · 50 ซม. ≈ 1.7 ซม. · 1 ม. ≈ 3.5 ซม.**
⇒ **ยิ่งตั้งมือถือไกล ยิ่งได้บรรทัดอ่านล่วงหน้าเยอะขึ้นฟรีๆ** (บอก user ด้วยทุกครั้ง ไม่ใช่แก้แต่โค้ด)

**ที่ทำไปแล้ว** (`web/components/posts/ScriptTeleprompter.jsx`):
แถบบทชิด `top-0` · สูงตามจำนวนบรรทัดจริง · เลือก 1/2/3 บรรทัดก่อนกดอัด (default 2)
· ย้ายปุ่มปิด+ตัวจับเวลาลงล่าง · ไล่เฉดจางก้นแถบ · **เต็มจออัตโนมัติตอนเข้าโหมดอัด**
(บนคอมแถบที่อยู่กินขอบบน ~100px มากกว่าความสูงแถบบททั้งแถบ · iOS ไม่รองรับ ข้ามเงียบๆ)
วัดจริงที่จอ 375×812: **จากเดิมเริ่ม 60px สูง 308px → เหลือเริ่ม 0px สูง 138px**

**เกณฑ์ผ่าน (ต้องดูจากคลิปที่อัดออกมา ไม่ใช่จากจอตอนอัด):** ดูคลิปแล้วไม่รู้สึกว่ากำลังอ่าน
· ถ้ายังเห็นตาขยับ → ลดเหลือ 1 บรรทัด + ตั้งมือถือไกลขึ้น ก่อนจะไปแก้โค้ดอย่างอื่น
⚠️ ห้ามสรุปจาก code/screenshot — ต้องให้ user อัดจริงแล้วดูคลิปตัวเอง

### ✅ เสร็จ+ขึ้น prod แล้ว: บทกว้างเกินบนจอคอม + พื้นหลังทึบ (`1639dd9`)
`max-w-[46ch]` คุมความยาวบรรทัด · `bg-black/40` + text-shadow เห็นหน้าตัวเองทะลุขึ้นมา
⛔ ถ้าอ่านไม่ออกบนพื้นสว่าง ให้เพิ่มเงาตัวอักษร **อย่าเพิ่มความทึบพื้นกลับ**

### ✅ เสร็จ+ขึ้น prod แล้ววันนี้: **bug-492 กล้องซูมเข้าหน้า** (`77f7611`)
อาการ: เปิดโหมดอัดแล้วเห็นแค่หน้าผากถึงจมูก ทั้งมือถือและเว็บแคมคอม

**ราก:** ขอกล้อง `1080×1920` เพราะคิดว่า "จะอัดแนวตั้งต้องขอแนวตั้ง" — **มันกลับกัน**
กล้องหน้าไอโฟนติดตั้งตะแคง เลขนี้ถูกตีความในพิกัดเซนเซอร์ที่ยังไม่หมุน พอเบราว์เซอร์หมุนภาพ
เฟรมจริงเลยเป็น**แนวนอน** แล้วโดน `object-cover` ยัดใส่กรอบเต็มจอแนวตั้ง = ครอปมหาศาล

**⛔ ห้ามพลาดซ้ำ 2 ข้อ (ลง cerebrum + bug-492 แล้ว):**
- **ห้ามเชื่อ `track.getSettings()`** — บน iOS มันอ้าง 1080×1920 แนวตั้งทั้งที่ภาพจริงแนวนอน
  ตัวจริงคือ `video.videoWidth/videoHeight` (ที่ตาเห็น + ที่ MediaRecorder อัด)
- **ห้ามเปลี่ยน constraint กลับเป็น `1080×1920`** — ต้องขอ `1280×720` ถึงได้เฟรม 720×1280 แนวตั้ง

**ที่แก้:** constraint → `1280×720` · วัดเฟรมจริงผ่าน `onLoadedMetadata`+`onResize`
· กรอบพรีวิว = สัดส่วนเฟรมจริง + `object-contain` (กฎเดียวใช้ได้ทั้งมือถือ/คอม)
· ปิด track เก่าก่อนขอใหม่ (ไม่งั้นกดครั้งแรกกับครั้งสองได้คนละสัดส่วน)

**ยังไม่ได้ทดสอบ:** Android · เว็บแคม Linux (เครื่องอยู่บ้าน) — กฎปรับตามเฟรมจริงอยู่แล้ว น่าจะรอด
**ค้าง:** ลบ `web/app/dev/camtest/` (หน้าทดสอบชั่วคราว) เมื่อยืนยันครบ

### 💡 บทเรียนวิธีทำงานจาก session นี้ (เสียเวลาไปทั้งวันกับการเดา)
ตั้งสมมติฐานผิดติดกัน 3 รอบเพราะเดาจากโค้ด (driver crop → CSS crop → capture crop)
**ที่จบเรื่องคือทำหน้าเทียบหลายแบบในหน้าเดียวแล้วให้ user กดจริง** (`/dev/camtest`)
➜ เรื่องกล้อง/สื่อบนเครื่องจริง: ยัดตัวเลือกให้ครบแล้ว deploy รอบเดียว อย่า deploy ทีละสมมติฐาน

---

## ⏭️ คิวถัดไปของสาย faceless (ตามที่ตกลงไว้ ยังไม่เปลี่ยน)
1. **[user ต้องลองเอง] เอาคลิปที่อัดไปโพสต์ขึ้นเพจจริง 1 คลิป** — เสี่ยงสุด ถูกสุด ยังไม่เคยพิสูจน์ปลายท่อ
2. **แก้ "บทไม่เข้าปาก"** — คอขวดจริงจากการสัมภาษณ์ user คือ**บท** ไม่ใช่เครื่องมือ
   ปรับ `head` ของ slot `posts.script` ใน `config/aiPrompts.js` + ปุ่ม "เกลาย่อหน้านี้" (ยังไม่ออกแบบ)
3. **โหมดไม่ออกหน้า (faceless จริงๆ)** — เสียง + ภาพนิ่งจากคลัง · ของใหม่ทั้งก้อน **ต้องคุยสเปกก่อน**
4. ⛔ **ขั้น 2 (ถอดเสียง/ซับ) ยังไม่ใช่ตอนนี้** — เกณฑ์คือทำคลิปจริง 3-5 คลิปก่อนแล้วค่อยดูว่าอะไรน่ารำคาญสุด

## ⛔ ข้อตกลงสายงานคลิป (ห้ามเปลี่ยนเอง)
- ห้ามให้ AI แต่งเนื้อหาใหม่ในบทพูด — แปลง*รูปแบบ*จาก body เท่านั้น
- **ห้ามเสนอ TTS / avatar / เจนภาพประกอบ** · ภาพประกอบต้องมาจากคลังรูปจริงของ user
- user **ไม่เข้าใจศัพท์เทคนิคเรื่องคลิป** → อธิบายด้วยภาษาคน
- อัด**นอกบ้าน มือถือเครื่องเดียว** → ทางที่ต้องมีจอที่สองตกไปหมด
- ⛔ ห้ามผูก "เลื่อนบทจบ" กับ "จบเทค" (bug-491) · ห้ามใส่ปุ่มปรับ**ขนาดตัวอักษร**ระหว่างอัด (บทรีโฟลว์)
- ⛔ ห้ามให้ client ลบคลิปเก่าก่อนอัปตัวใหม่ — ใช้ `?replace=1` (เขียนใหม่สำเร็จก่อน ค่อยสลับแถว ค่อยลบเก่า)

## 🧪 เครื่องมือ
- **`scripts/dev/recorderRepro.mjs`** — Chrome headless + กล้อง/ไมค์ปลอมบน localhost กดอัดจริงแล้ววัดผลได้
  ต้องมี dev server: `cd web && NEXT_DIST_DIR=.next-audit PORT=3100 npx next dev -p 3100`
  ⛔ เขียนทับบทพูดของโพสต์ที่ระบุ (default `POST=793`) — ใช้กับโพสต์ draft ทดสอบเท่านั้น
  · รายงาน `ตำแหน่งแถบบท` (ห่างขอบบนกี่ px / สูงกี่ px) = ตัวเลขที่ใช้ตัดสินเรื่องตาเหลือบ
  · **ถ้าขึ้น "เปิดกล้องไม่ได้ ต้องอนุญาต…" ให้เช็ค chrome ค้างก่อน** (bug-498) —
    `pkill -f "remote-debugging-port=9333"` · ตอนนี้สคริปต์มียามเตือนให้แล้ว
- **`node scripts/dev/mobileAudit.mjs --routes /หน้าที่แก้`** — ล็อกอินเองได้ ต้องมี dev server รันก่อน

## ⚠️ รู้ไว้
- **`getUserMedia` ทำงานเฉพาะ https หรือ localhost** — เปิดผ่าน `http://<ip>:3000` บนมือถือกล้องไม่ขึ้น
  · user อยู่นอกบ้าน = ทดสอบผ่าน prod เท่านั้น (tunnel รันเองไม่ได้ โดน classifier บล็อก)
- **เบราว์เซอร์ถามสิทธิ์กล้องทุก reload** เป็นปกติ (Safari/Brave default) แก้ที่ site settings ฝั่ง user
- **หลังรีบูตเครื่อง dev → ssh เข้า prod พัง** (`agent refused operation`) เพราะ gnome-keyring ล็อก
  ให้ user รัน `ssh tee@202.183.141.78 true` ในเทอร์มินัลตัวเองครั้งเดียว
- **prod ยังมี GitHub token ฝังใน remote url** — ออก token ใหม่ต้องไปแก้ที่ prod ด้วย
- dev server ของ user รันพอร์ต 3000 — **ห้าม `npm run build` ตอนมันรันอยู่** (ชน `.next` ทำเว็บ 500)
- หนี้: คลิปที่อัดในเว็บไม่มี `source_url` → `services/postsRetention.js:46` ไม่เก็บกวาดให้ (จดใน PENDING แล้ว)

## ➜ ค้างไว้ (ไม่ด่วน)
1. `ALTER TABLE kanban_boards ALTER COLUMN teamspace_id SET NOT NULL;` — เขียน migration แล้ว **ยังไม่รันบน prod**
2. Posts โหมด AI "ร่างตามคำแนะนำ" (`5c73c8b`) — user ยังไม่กดทดสอบ
3. แท็บ "นำเข้าแล้ว" ยังไม่มีลิงก์ไปการ์ด KB-xxx · ยังไม่มีปุ่ม "ยกเลิกการนำเข้า"
4. หนี้ i18n ทั้งโซน posts (`PostEditor.jsx`) — ดู `md/PENDING.md`

## 🧷 ของค้างในเครื่อง dev ที่ไม่ใช่ของ Claude
`utils/quoteStyleKeys.js` · `web/lib/quoteStyles.js` · `web/locales/th.json` — แก้คำ
**"การ์ดคำคม" → "การ์ดโควต"** ค้างอยู่ **เป็นงานที่ user แก้เอง ห้าม commit ให้ ห้ามรื้อ**
