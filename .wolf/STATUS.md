# STATUS — 2026-09-05 (เย็น)

## 🆕 ย่อรูปทุกทางเข้าของ posts (`1caf562` · **ขึ้น prod แล้ว 2026-09-05 17:05** · user ยังไม่กด "ลองใหม่")

**อาการที่ user เจอ:** โพสต์ https://pplevolunteers.org/posts/1051 ยิงแล้วล้ม **ทั้ง 5 แพลตฟอร์ม**
(X "62857454 bytes too large" · FB "reduce the amount of data" · Threads/IG timeout ·
ห้องข่าว Discord "Request entity too large")

**ต้นเหตุ (ตรวจจาก DB+ดิสก์ prod จริง):** สื่อชิ้นเดียวในโพสต์นั้น — การ์ดคำคม PNG **6936×8670 = 35 MB**
เพราะสไตล์ "มีรูป" เรนเดอร์ **เท่าขนาดรูปพื้นหลัง** (พื้นหลังเป็นรูป 60 ล้านพิกเซล 7 MB)
ติดลายน้ำแล้ว re-encode เป็น PNG อีกรอบ = 60 MB · ทั้งท่อไม่เคยย่อรูปเลยตั้งแต่วันแรก
เพดานที่มีคือ **ขนาดไฟล์ขาเข้า 12 MB** ซึ่ง jpeg 60 MP ผ่านสบาย (buglog `bug-465`)

- `utils/imageDownscale.js` (ใหม่) — ด้านยาว ≤ 2048 · เกิน 4 MB → jpeg q88 · png มี alpha คง png
  · gif/คลิป/เสียงไม่แตะ · รูปในกรอบอยู่แล้วคืน buffer เดิม (ไม่ re-encode ทิ้งคุณภาพ)
- ต่อ 4 จุด: `quoteStyles.js prepImage()` · `publishPipeline.js prepareImages()` (ก่อน+หลังลายน้ำ)
  · `utils/postsStorage.js saveBuffer()` (ตะกร้าดิสฯ) · `web/lib/postsStorage.js savePostFile()`
- **คลังภาพต้องย่อก่อนคิด sha256/width/height/bytes** ไม่งั้นค่าใน DB ไม่ตรงไฟล์บนดิสก์ → `shrinkForStorage()`
- เทส noise 6936×8670: การ์ด 3.29 MB → หลังลายน้ำ 2.77 MB · web build ผ่าน · vitest 506 ผ่าน
- ✅ deploy แล้ว (pull → build 0 warning → pm2 restart pple-web/pple-dcbot) · **เหลือให้ user กด "ลองใหม่" ที่โพสต์ 1051**
  — ท่อย่อตอนยิง ทำให้ใช้การ์ดเดิมที่ค้างอยู่ได้เลย ไม่ต้องทำการ์ดใหม่
- ⚠️ `1caf562` แก้ warning ตอน build บน prod: `serverExternalPackages` ไม่ครอบไฟล์นอก `web/` → webpack ลาก sharp เข้ามา bundle · ต้องโหลดผ่าน createRequire ที่ราก
- ⬜ cases/kanban ยังไม่ต่อตัวย่อ + ไฟล์เก่า 26 ใบเกิน 8 MB ค้างดิสก์ → `md/PENDING.md`

## 🆕 Posts — โหมด AI "ร่างตามคำแนะนำ" (`5c73c8b` · local เท่านั้น · **user ยังไม่กดทดสอบ**)

AI หาจุดที่ควรแก้ที่สุดจุดเดียวแล้วแก้ให้เลยในคลิกเดียว · เกิดจาก user ถามว่า "ถ้า AI ให้คำแนะนำได้
ทำไมไม่มีโหมดร่างตามคำแนะนำนั้นไปเลย"

- `config/aiPrompts.js` slot ใหม่ `posts.guided` → JSON `{advice, body}` · endpoint `web/app/api/posts/ai/guided/route.js`
- สิทธิ์ AI: รื้อโครง/สลับย่อหน้า/เขียน hook ใหม่ได้ **ห้ามเติมข้อเท็จจริง/ตัวเลข/ชื่อ/คำพูดใหม่** (user เคาะ)
- **ห้ามใส่ ConfirmDialog กลับ** — user สั่ง "กดทีเดียวจบ" · ตาข่ายคือ `snapshotCurrent()` หลัง AI สำเร็จ
- **ห้ามเก็บ advice ลง `post_ai_suggestions`** — การ์ดข้อเสนอแตกแค่ 2 ทาง (review / 6 หมวด caption)
  แถวชนิดใหม่จะได้การ์ดเปล่ามีแต่วันที่
- ดรอปดาวน์ AI แยก `<optgroup>` "แก้เนื้อหาให้" (5) vs "ไม่แตะเนื้อหา" (2) · คำอธิบายอยู่ในตาราง `AI_MODES`
  ที่เดียว tooltip อ่านจากที่นั่น
- **ตัด CTA ออกจาก `posts.caption` ถาวร** (user สั่ง) — save ลง DB แต่ UI ไม่เคย render
- ต้องกดลองจริงที่ `/posts/{id}` → เลือก "ร่างตามคำแนะนำ" → "ให้ AI ช่วย" (ยังไม่มีใครยิงจริงสักครั้ง)

## ✅ คัดกระทู้ดิสฯ เข้า KANBAN — **ใช้งานจริงบน prod แล้ว** (prod อยู่ที่ `eca1158`)

user ใช้จริงไปแล้ว: **นำเข้า 12 · ไม่เอา 14 · เหลือรอคัด 229** — ทยอยคัดวันละนิดได้เรื่อยๆ ไม่ต้องรีบ

`https://pplevolunteers.org/kanban/import/forum`

### commit ของงานนี้ (เรียงเก่า→ใหม่)

| commit | อะไร |
|---|---|
| `87163a0` | ไฟล์แนบรูปบนการ์ด — `kanban_card_attachments` (+`field_id` เผื่อ custom upload field) |
| `8b962c8` | ตารางพัก `kanban_forum_import` + `prepForumImport.mjs` (AI) + หน้าคัด |
| `c372bd3` | แก้วันจัดงานเองได้ · แยก "ตั้งใจให้ว่าง" จาก "ยังไม่แตะ" · บั๊กโซนเวลา · cast พารามิเตอร์ |
| `0176287` | ผู้รับผิดชอบหลายคน + กันนำเข้าใบที่ธงว่าซ้ำ (>= 0.9) |
| `5679caa` | **รูปเก็บทั้งกระทู้ ไม่ใช่แค่ข้อความเปิด** + `refreshForumImages.mjs` ตามเก็บย้อนหลัง |
| `9e3ba6d` | โฟลเดอร์ไฟล์แนบไม่ขึ้นกับ cwd (สคริปต์รันจากรากโปรเจกต์แล้ว EACCES) |
| `5cedf3f` | เลือกสถานะการ์ดตอนคัดได้ (ไม่ยัด done ให้ทุกใบแล้ว) |
| `b2e8fc4` | เปิดหน้าคัดให้กรรมการจังหวัด (`canImportForum`) |
| `eca1158` | ปุ่ม "นำเข้าใบนี้" ประจำแต่ละใบ |

### สถานะข้อมูลบน prod
- `dc_forum_posts` 3 ห้องครบ 256 · `kanban_forum_import` 255 แถว AI ครบ
- กระทู้ที่มีรูปให้ดึง 176 ใบ รวม 533 รูป · การ์ดที่นำเข้าแล้วมีไฟล์แนบ 22 ไฟล์ (เพดาน 4/ใบ)

### กติกาที่เคาะไว้ (อย่ารื้อโดยไม่ถาม)
- วันของการ์ด: `created_at`/`start_at`/`updated_at` = วันตั้งกระทู้ · `due_at` = วันจัดงานที่ AI ดึงได้
  · `completed_at` = วันจัดงาน (ไม่มีก็วันตั้งกระทู้) **เฉพาะสถานะที่ปิดงานแล้ว** — งานที่ยังไม่จบต้องไม่มีวันเสร็จ
- **ไม่บังคับมีผู้รับผิดชอบ** · ห้ามยัดคนปลอมซ้ำรอย `backfillCaseThreads`
- `ai_*` = ข้อเสนอ · `pick_*` = คนเคาะ · รัน `--refresh` ทับได้โดยของที่คนแก้ไม่หาย
- ใบที่ `dup_score >= 0.9` ต้องกดยืนยันก่อนถึงนำเข้าได้ (เคยพลาดสร้างการ์ดซ้ำ KB-1267 มาแล้ว)
- ลบการ์ดถาวรยังเป็น admin เท่านั้น (ไม่ได้เปิดตามกรรมการจังหวัด)

### รอบ 2026-09-05 บ่าย — `cfe0f64` (**บน prod แล้ว**) + `99d8fde` (push แล้ว · **prod ยังไม่ pull**)

- **ห้อง "เลือกตั้ง" `1258076247700013146`** เพิ่มในโค้ดแล้ว (prepForumImport CHANNELS + ชิปกรองในหน้าคัด + คีย์ i18n)
  ⚠️ **ข้อมูลยังไม่มี** — ต้องรันบน prod 2 สคริปต์: `scripts/data/backfill-forum.js --guild <GUILD_ID> --channel 1258076247700013146`
  แล้วตามด้วย `scripts/kanban/prepForumImport.mjs --org 1 --channel 1258076247700013146` (ยิง AI = เสียเงินตามจำนวนกระทู้)
- **หลายมือช่วยกันคัด** — migration `1788630000000_kanban-forum-import-touched-by`
  (`touched_by`/`touched_at`) · ใบที่มี `pick_*` ใดๆ = "แก้แล้ว" ขึ้นป้าย "<ชื่อ> แก้แล้ว · <เวลา>"
  + **ลอยขึ้นบนสุดของรายการเสมอ** + ชิปกรอง "มีคนแก้แล้ว"
  · user เคาะ: **ไม่เอาปุ่ม "ตรวจแล้ว" แยก** (ตรวจจับจากการแก้พอ) · **ปุ่มนำเข้าไม่ต้องหวง** เปิดให้ทุกคนที่เข้าหน้านี้ได้เหมือนเดิม
- กล่อง "วิธีคัด" 6 ข้อบนสุดของหน้า (คีย์ `kanbanImport.rules.items`) — คนใหม่ต้องอ่านจบใน 10 วินาที ห้ามยาวกว่านี้
- คำว่า "วันจัดงาน" เปลี่ยนเป็น **"กำหนดส่ง"** ทั้งหน้า (user: เข้าใจตรงกว่า)
- ⚠️ `pick_no_assignee` **ถูก DROP ไปแล้ว** ตั้งแต่ `1788542000000` — อย่าอ้างถึงใน SQL ใหม่ (เจ็บมาแล้วรอบนี้)

### สถานะ prod ล่าสุด (เช็คจาก DB จริง 2026-09-05 บ่าย)

- prod HEAD = `936aca9` → **มี `cfe0f64` แล้ว · ยังไม่มี `99d8fde`** (การ์ดจาง + ลิงก์ธงซ้ำ + กฎชุดใหม่)
  → เหลือ 3 คำสั่ง: `git pull origin master` · `cd web && npm run build` · `pm2 restart pple-web pple-dcbot --update-env`
  (⛔ `git pull` ผ่าน ssh **โดน auto-mode classifier บล็อก** — Claude รันเองไม่ได้ ต้องให้ user รัน หรือเพิ่ม permission rule)
- migration `1788630000000_kanban-forum-import-touched-by` **รันบน prod แล้ว** (06:48Z) คอลัมน์ `touched_by`/`touched_at` มีครบ
- **ห้องเลือกตั้งพร้อมใช้แล้ว** — `prepForumImport.mjs --org 1 --channel 1258076247700013146` รันครบ **52/52**
  (รอบแรก 45 ใบ · พลาด 7 ใบเพราะ "Connection error" ของ Anthropic → รันซ้ำอีกรอบเก็บครบ · สคริปต์ข้ามใบที่ทำแล้วเอง)
- `kanban_forum_import` ตอนนี้: คณะทำงาน 127 · อำเภอ 91 · สมาชิกพรรค 37 · เลือกตั้ง 52
- **KB-1284 (`kanban_cards.id=1286`)** — `created_at` บน prod ยังเป็น 2026-09-04 20:20 (ตรงกับที่เว็บโชว์)
  · user ยืนยันเองว่า **save แต่ไม่ได้ commit บน DBeaver** · ไม่มี trigger บน `kanban_cards` · การ์ดใบนี้สร้างมือ ไม่ได้มาจากการคัดกระทู้

## ➜ ค้างไว้ให้ทำต่อ (ไม่ด่วน)

1. **แท็บ "นำเข้าแล้ว" ยังไม่มีลิงก์ไปเปิดการ์ด KB-xxx** — เสนอไว้แล้ว user ยังไม่ตอบ
2. **ยังไม่มีปุ่ม "ยกเลิกการนำเข้า"** — นำเข้าผิดต้องไปลบการ์ดเองที่ /kanban แล้วแถวจะค้างชี้การ์ดที่ไม่มีอยู่
   (เสนอทำแบบมีเบรก: ลบได้เฉพาะการ์ดที่ยังไม่มีใครแตะหลังนำเข้า)
3. **AI ตกไป 1 ใบจาก 256** — รัน `prepForumImport.mjs --org 1` ซ้ำได้ มันข้ามใบที่ทำแล้ว
4. `caseUploads.getCaseUploadDir()` / `cropDocument.getUploadPath()` **ยังมีบั๊ก cwd แบบเดียวกับที่เพิ่งแก้**
   — ยังไม่มีสคริปต์ไหนเรียกจากรากโปรเจกต์เลยไม่เจ็บ แต่ถ้าจะเขียนสคริปต์แตะไฟล์แนบเคส/เอกสาร ต้องแก้ก่อน

## ⚠️ รู้ไว้
- **dev server ที่เครื่อง user ดับอยู่** (ตอนจบ session) — ถ้าจะรัน mobileAudit ให้เปิดตัวทดสอบพอร์ต 3100
  ด้วย `NEXT_DIST_DIR=.next-test` แล้วส่ง `--base http://localhost:3100`
- เครื่อง dev มี `ANTHROPIC_API_KEY` เก่าค้างใน `~/.bashrc` — สคริปต์ต้องโหลด env ผ่าน
  `scripts/smoke/_envload.mjs` (มี `override: true`) เท่านั้น ไม่งั้นเจอ "credit balance is too low" หลอกๆ
