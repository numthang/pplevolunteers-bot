# STATUS — 2026-09-04 (เที่ยง)

## ✅ deploy prod แล้ว — prod อยู่ที่ `4434cdc`

`git pull` + `npm run migrate up` + `web build` + `pm2 restart pple-web pple-dcbot` ครบ ·
log หลัง restart ไม่มี error ใหม่ (ที่เห็นใน error.log เป็นของเก่า: Server Action ค้างจาก
deploy ก่อนหน้า + Meta/Threads API ของบอทวันที่ 09-03) · เว็บตอบ 200

### 1. ผู้รับผิดชอบของงานสื่อ — ร่างส่วนตัวกดรับงานเองได้ (`9db3be5`)

user ทัก: "ผมกดรับเรื่อง posts ส่วนตัวไม่ได้ถูกไหม มันแปลกๆ" — แปลกจริง เพราะการ์ด
ร่างส่วนตัวมีคนเห็นคนเดียวคือเจ้าของ แล้วยังบอกเขาว่า "ยังไม่มีผู้รับผิดชอบ" · ถ้าไป
promote ระบบก็ใส่ชื่อเขาให้เองอยู่ดี = บังคับทำ action ที่ย้อนไม่ได้เพื่อผลลัพธ์เดิม

- `canAssignPost` (ใส่**คนอื่น** · ต้อง org) แยกจาก `canSelfAssignPost` (**ตัวเอง** ·
  org หรือร่างส่วนตัวของตัวเอง) · ด่านรวมที่ `postAssignBlock()` ให้ทั้ง 3 ประตูใช้ร่วมกัน
- ⛔ **`promoteToOrg` เลิก seed `created_by`** — promote ตอบ "ใครเห็น" ไม่ใช่ "ใครทำ"
  (จุดสุดท้ายที่ยังฝ่าฝืนกฎ `links.js` §SOURCE_SQL ที่เฟส C ตั้งไว้เอง)
- ปุ่ม "รับงานนี้ / ลงมือด้วย / ถอนตัว" ใน `PostMetaPanel` + ย้ายแถวผู้รับผิดชอบลงล่างสุด
  เป็นสองบรรทัด (เดิม `flex justify-between` แตกเมื่อมีชื่อไทยหลายคน)
- migrate `PostMetaPanel` ทั้งไฟล์เป็น `t()` (~35 คีย์ใหม่ใน `posts.meta`)

verify: vitest 511 · build · สโมค `kanbanPostSync` +11 เคส · HTTP จริงบน dev ที่ล็อกอินแล้ว
(กดรับ/ถอนตัว/บล็อกคนอื่น/claim จากบอร์ดแล้ว `/posts` ตรงกัน) · mobileAudit + ดูภาพจริง

### 2. `4434cdc` — % เช็คลิสต์บนการ์ด kanban + migration `completed_at`

migration `1788490304371` **Applied แต่แตะ 0 แถวบน prod** — ตรวจก่อนรันแล้ว: scope เข้า
เงื่อนไข 100 แถว แต่ทุกแถว `completed_at` ตรงกับ `cases.created_at` อยู่แล้ว (migration
`1788454524874` รอบก่อนจัดการไปหมดแล้ว) · **ไม่ใช่ WHERE เพี้ยน** — นับก่อน/หลังยืนยันแล้ว

---

## ➜ ที่ user ควรกดเช็คบน prod

1. `/posts/<ร่างส่วนตัวของตัวเอง>` → ต้องมีปุ่ม **"รับงานนี้"** และแถว "ผู้รับผิดชอบ" อยู่ล่างสุด
2. กดรับแล้วเปิด `/kanban` → การ์ดใบเดียวกันต้องขึ้นชื่อเราด้วย (สำเนา sync สองทาง)
3. กด "เปิดให้ทีมเห็น" บนร่างที่**ยังไม่ได้กดรับ** → ใบนั้นต้องขึ้นเป็น "ยังไม่มีคนรับ" (ตั้งใจ)
4. โพสต์ที่มีผู้รับผิดชอบหลายคนบนมือถือ → ชื่อต้อง wrap ลงบรรทัดใหม่ ไม่ดันจอ

## ⚠️ ค้าง / รู้ไว้
- `canPromoteToOrg` ปิดปุ่ม "เปิดให้ทีมเห็น" เมื่อโพสต์เคยยิงลงโซเชียลแล้ว (`post_social_history`)
  — กฎเดิม ไม่ใช่ของใหม่รอบนี้ · ถ้าดูแล้วไม่ควรเป็นแบบนั้นค่อยคุยแยก
- ร่างส่วนตัวเก่าที่มีอยู่ก่อนวันนี้ ไม่เคยมีโอกาสกดรับ → promote แล้วจะขึ้น "ยังไม่มีคนรับ"
  (ข้อความ confirm เตือนล่วงหน้าแล้ว)
- `/kanban` ยังไม่เคยมีใครไล่กดทดสอบจริงบน prod (ค้างจาก STATUS รอบก่อน)
