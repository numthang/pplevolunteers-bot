# STATUS — 2026-09-08

## ✅ เควสต์ล่าสุด: Posts — บทพูด + เครื่องช่วยอ่านหน้ากล้อง
`77bfbfc` `36ec756` · **local เท่านั้น · ยังไม่ push · user ยังไม่กดทดสอบ**
แผนเต็ม `/home/tee/.claude/plans/rosy-wibbling-wand.md`

**ขั้นที่ 1 ของสายงานคลิป** — user ต้องทำคลิปเพราะคนเสพวิดีโอมากกว่าอ่าน แต่**ตัดต่อไม่เป็นและไม่ชอบทำคลิป**
และปฏิเสธทางลัดสาย AI ทุกทาง เพราะยึดหลัก **ของจริงเป็นแกน · AI เร่งงานรอบๆ · คนตัดสินใจปลายทาง**
→ ทางที่ถูกที่สุดคือ **ไม่ต้องตัดต่อเลย**: มีบทให้อ่านจากจอ แล้วอัดจบในเทคเดียว
อ้างอิง **Descript** แต่ลอกแค่แนวคิดเดียว (บทเป็นตัวตั้ง ไม่ใช่ไทม์ไลน์) เหมือนที่ลอกกระดานจาก Notion

| ของที่ได้ | ที่ไหน |
|---|---|
| slot `posts.script` | `config/aiPrompts.js` — ประโยคสั้น · ตัดวงเล็บ/บุลเล็ต/อีโมจิ/hashtag · ตัวเลขเป็นคำอ่าน · ย่อหน้าตามจังหวะหายใจ |
| endpoint | `web/app/api/posts/ai/script/route.js` — **ไม่เขียน DB เอง** (bug-071) คืนข้อความให้ editor เซฟด้วย token ของมัน |
| หน้าอ่าน | `/posts/[id]/script` → `web/components/posts/ScriptTeleprompter.jsx` |
| ทางเข้า | `PostEditor.jsx` — โหมด "ทำบทพูด" ในเมนู AI + ปุ่ม "เปิดบทพูด" |
| สโมค | `node --import ./scripts/smoke/_envload.mjs scripts/smoke/postsScript.mjs` |

**ไม่มี migration** — เก็บที่ `post_episodes.bodies->>'script'` (jsonb ที่มีอยู่แล้วแต่ไม่เคยมีใครใช้)
⚠️ ต้อง **merge** ของเดิมใน `bodies` เสมอ เขียนทับทั้งก้อน = คีย์อื่นหายเงียบๆ

**verify ผ่านแล้ว:** build ✅ · 507 tests ✅ · mobileAudit `/posts/1047/script` exit 0 ✅ ·
smoke ✅ (8 ด่านรูปแบบ + round-trip ของ `bodies` + lockToken เก่าถูกปฏิเสธ)
**ตัวอย่างของจริงบน local:** โพสต์ **1047** (บทความนกเงือกบางกะม่า 2,719 ตัวอักษร → บทพูด 1,305)

**บทเรียนรอบแรก (ปิดช่องใน `head` แล้ว + มีด่านดักใน smoke ทั้งคู่):**
AI **สร้างช่อง `[ใส่ตรงนี้ — …]` ที่ต้นฉบับไม่มี** และ **เติมวลี "กำลังเร่งดำเนินการต่อเนื่อง"** ที่ต้นฉบับไม่ได้พูด
— สองอย่างนี้คือสิ่งที่ user รับไม่ได้ที่สุด

### ➜ ค้างอยู่ตรงนี้พอดี
1. **คำลงท้าย ครับ/ค่ะ ไม่ตรงกับผู้พูด** — บทของ 1047 ออกมาเป็น "ค่ะ" เพราะต้นฉบับเขียนแบบนั้น แต่ user พูด "ผม/ครับ"
   **ถาม user ไปแล้ว ยังไม่ตอบ** ว่าจะแก้ทางไหน: (ก) แก้เองในหน้าอ่าน (ข) ใส่กฎใน `head` (ค) ตั้งค่าที่ AI backoffice
   ⛔ ห้ามเดาแทน — เป็นเรื่องเฉพาะบุคคล และระบบเป็น multi-tenant
2. **ยังไม่ได้กดทดสอบจริง** — เกณฑ์ตัดสินคือข้อ 7 ของแผน: **อัดคลิปหนึ่งคลิปโดยอ่านจากหน้านี้ แล้วดูว่าได้เทคเดียวจบไหม**
   ถ้ายังต้องอัดหลายเทค = ปัญหาอยู่ที่บทพูด → ไปปรับ `head` **ก่อน**ขึ้นขั้นที่ 2
3. **เกณฑ์ขึ้นขั้น 2:** ทำคลิปจริง 3-5 คลิปก่อน แล้วดูว่าอะไรน่ารำคาญที่สุด — ไม่ใช่เดาล่วงหน้า
   (ขั้น 2 = ถอดเสียง+ซับ · ขั้น 3 = แก้คลิปด้วยการลบประโยค · ขั้น 4 = รูปจริงขึ้นตามจังหวะ)

### ⛔ ข้อตกลงที่ห้ามเปลี่ยนเอง (สายงานคลิป)
- **ห้ามให้ AI แต่งเนื้อหาใหม่ใน slot นี้** — แปลง *รูปแบบ* จาก body ที่มีอยู่เท่านั้น
- **ห้ามเสนอ TTS / avatar / เจนภาพประกอบ** — user ปฏิเสธชัดเจน ไม่ใช่เพราะยังไม่ถึงคิว
- ภาพประกอบในอนาคตต้องมาจาก **คลังรูปจริงของ user** ไม่ใช่ภาพ AI (user ทำข่าวด้วยรูปจริงเสมอ)
- งานเก็บรายละเอียดกราฟิกนิ่งอยู่บน **Canva** ของ user — ไม่ต้องย้ายเข้าระบบ
- user บอกเองว่า **ไม่เข้าใจศัพท์เทคนิคเรื่องคลิป** → อธิบายด้วยภาษาคน ห้ามถามเป็นตัวเลือกเชิงสถาปัตยกรรม

---

## ✅ ก่อนหน้า: KANBAN ชั้น teamspace — **ขึ้น prod แล้ว** (prod = `5563a5c`)
`org > teamspace > boards > cards` · แผน `/home/tee/.claude/plans/kanban-multi-board-dreamy-quilt.md`
ตรวจหลัง deploy: teamspace "ทีมราชบุรี" · บอร์ดที่ยังไม่มีทีม = 0 · การ์ด 1,410 ใบเท่าเดิม

**เหลือ:** `ALTER TABLE kanban_boards ALTER COLUMN teamspace_id SET NOT NULL;`
(NULL = 0 ทั้ง local และ prod แล้ว · เขียนเป็น migration ใบใหม่แล้ว deploy รอบถัดไป)

**⛔ ข้อตกลง:** ใช้คำว่า **teamspace** ทับศัพท์ · **custom field ผูกกับ "กระดาน" ไม่ใช่ teamspace (user ยืนยัน ห้ามเสนอย้ายอีก)** ·
รอบนั้น**ไม่มีด่านสิทธิ์เลยโดยตั้งใจ** — จะกันจริงต้องแก้ `web/db/kanban/scopeSql.js` แล้ววางลง 3 จุดพร้อมกัน
(`canViewTeamspace` · `listCards` · `getCardForViewer`) ทำครึ่งเดียว = ความเป็นส่วนตัวปลอม ·
ถ้าจะกันสักจุด **กันการ์ดเคสร้องเรียน 176 ใบก่อน** · ห้ามเพิ่ม `is_guild_default` / `kanban_board_channels` / `kanban_columns`

---

## ➜ ค้างไว้ (ไม่ด่วน)
1. **Posts โหมด AI "ร่างตามคำแนะนำ"** (`5c73c8b`) — local เท่านั้น user ยังไม่กดทดสอบ
2. แท็บ "นำเข้าแล้ว" ยังไม่มีลิงก์ไปเปิดการ์ด KB-xxx · ยังไม่มีปุ่ม "ยกเลิกการนำเข้า"
3. AI ตกไป 1 ใบจาก 256 — รัน `prepForumImport.mjs --org 1` ซ้ำได้ มันข้ามใบที่ทำแล้ว
4. `caseUploads.getCaseUploadDir()` / `cropDocument.getUploadPath()` มีบั๊ก cwd แบบเดียวกับที่แก้ไปแล้ว
5. cases/kanban ยังไม่ต่อตัวย่อรูป (`web/lib/kanbanUploads.js`) · `gc-media.js` ยังไม่มีอะไรเรียกอัตโนมัติ
6. หนี้ i18n ทั้งโซน posts (`PostEditor.jsx` 764 บรรทัดไทย hardcode) — รายละเอียดใน `md/PENDING.md`

## ⚠️ รู้ไว้
- **dev server ของ user รันอยู่ที่พอร์ต 3000** — จะ build ต้องตั้ง `NEXT_DIST_DIR` แยก ไม่งั้นชน `.next` เดียวกันแล้ว dev server 500 ทั้งเว็บ
- เครื่อง dev มี `ANTHROPIC_API_KEY` เก่าค้างใน `~/.bashrc` → สคริปต์ต้องโหลด env ผ่าน `scripts/smoke/_envload.mjs`
- **prod ไม่มีปุ่ม "เพิ่มกระดาน" ใหญ่ๆ มุมขวาบนแล้ว** (user สั่งเอาออก) — สร้างการบ้าน = ปุ่ม + บนหัวกอง · สร้างกระดาน/teamspace = ในลิสต์เลือกที่ทำงาน
- git ในเครื่องนี้เคยเพี้ยน hash หายจาก reflog 2 รอบ — ก่อน deploy เช็ค `git log -1` ทั้ง 2 ฝั่งจริง
- ล็อกอินทดสอบ local: insert token ลง `org_login_tokens` ตรงๆ (ห้ามยิง endpoint = ส่งเมลจริง) · **curl ใช้ไม่ได้** ต้องผ่านเบราว์เซอร์จริง (next-auth ตั้ง cookie ด้วย JS)
