# STATUS — 2026-09-08

## ➜ เควสต์ปัจจุบัน: สายงานคลิป — รอผลทดสอบ `/dev/camtest` บนมือถือจริง

**ขั้นที่ 1 ผ่านแล้ว** — user อัดคลิปจริงแล้ว: **ไม่เกิน 3 เทค · ซ้อมก่อนแล้วเทคเดียวจบ**
→ สมมติฐานทั้งเส้น "มีบทให้อ่าน = ไม่ต้องตัดต่อ" **ยืนได้จริง** ไม่ต้องพิสูจน์ซ้ำอีก

### ปัญหาที่เหลือข้อเดียว: **ตาไม่ได้มองกล้อง**
บทอยู่คนละที่กับเลนส์ → แก้ได้ทางเดียวคือทำให้ **บทอยู่ใกล้เลนส์ที่สุด** (ลดมุมเหลือบ)

**ข้อจำกัดจริงของ user ที่ปิดทางเลือกไปหลายทาง (อย่าเสนอซ้ำ):**
- อัด**นอกบ้าน มีมือถือเครื่องเดียว** — คอมอยู่ในบ้าน + ใช้เสียงที่บ้านไม่ได้ตลอดเวลา
  → ทาง "สองเครื่อง (จอคอมโชว์บท + มือถืออัด)" **ตกไป** · teleprompter กระจกก็ต้องมีจอที่สอง ตกไปด้วย
- **เป้าหมายคือมือถือ** คอมเป็นแค่ของแถมที่ได้ประโยชน์ด้วย (user ย้ำเอง — อย่าเปลี่ยนเป็นเล็งคอม)
- แอปกล้องปกติใช้ไม่ได้เพราะสลับไปเปิดบทในเบราว์เซอร์แล้ว**กล้องหยุดอัด** (พฤติกรรม OS ปกติ)

### เหลือ 2 ทาง — ตัวชี้ขาดคือผลทดสอบ
| ทาง | ต้นทุน | ได้/เสีย |
|---|---|---|
| **ง. แอป teleprompter สำเร็จรูป** | 0 บรรทัด | คุณภาพวิดีโอดี (กล้องเนทีฟ) · แต่ต้องก็อปบทเอง + อัปคลิปเข้าโพสต์เอง |
| **ค. เขียนเครื่องอัดในเบราว์เซอร์เอง** | งานใหญ่ | บทอยู่ในระบบ + อัปเข้าโพสต์อัตโนมัติ (มือถือไม่เต็ม) · แต่คุณภาพด้อยกว่า + เสี่ยง Safari |

### ⏭️ ทำอะไรต่อ — **รอ user เปิดลิงก์เดียวนี้ก่อน**
**`https://pplevolunteers.org/dev/camtest`** (deploy แล้ว prod = `6a345d0` · ตอบ 200 แล้ว)
เปิดบนมือถือตัวเอง **+ ให้คนในทีมที่ใช้ไอโฟนลอง 1-2 เครื่อง** แล้วดู 3 อย่าง:
1. ภาพชัดพอเทียบกับแอปกล้องไหม 2. เสียงชัดไหม 3. บรรทัด **"ความยาวที่อ่านได้"** ขึ้นเป็นตัวเลขหรือ "อ่านไม่ได้"
(อ่านไม่ได้ = ไฟล์ไม่มี metadata ความยาว → เสี่ยงโพสต์ขึ้นเพจไม่ผ่าน ต้องแก้ก่อนใช้จริง)

**ผ่าน** → สร้าง ค. เล็งมือถือเต็มตัว · **ไม่ผ่าน** → ไป ง. แล้วลบ `/dev/camtest` ทิ้ง

### 🔧 ของที่เตรียมไว้แล้ว
- **`web/app/dev/camtest/page.js`** — หน้าทดสอบชั่วคราว (ไม่แตะ DB/API เลย) **ลบทิ้งเมื่อตัดสินใจเสร็จ**
- **branch `wip/browser-recorder`** — โค้ดเครื่องอัดเขียนค้างไว้ ~90% **ห้าม merge**
  ยังขาด: คำแปล th/en · **ช่องโหว่: ลบคลิปเก่าก่อนอัปโหลด = เน็ตหลุดแล้วเสียของทั้งสองทาง**

### ⛔ 3 ช่องโหว่ที่ต้องแก้ก่อนสร้าง ค. จริง (Opus ตรวจเจอ 2026-09-08)
1. **แนบคลิปได้โพสต์ละ 1 ชิ้น** (`MAX_VIDEO_PER_EPISODE`) → อัดซ้ำจะโดน 400
   แก้ที่ client ไม่ได้ **ต้องให้ `/api/posts/[id]/media/video` รับ "อัปทับ" ในคำขอเดียว**
2. **คุณภาพวิดีโอจาก getUserMedia ด้อยกว่ากล้องเนทีฟ** (ไม่มีกันสั่น/ปรับแสง) — เนื้อหาขึ้นเพจพรรค
3. **ไฟล์จาก MediaRecorder มักไม่มีข้อมูลความยาวฝัง** → ท่อโพสต์ FB/IG อาจพังตอนเผยแพร่
+ ควรลอก **"อัดไปอัปไป เป็นก้อนๆ"** จาก Loom/Riverside แทนการส่งทีเดียวตอนจบ (สำคัญกับเน็ตมือถือ)

---

## ✅ เสร็จแล้ว: บทพูด default ผู้พูดเป็นผู้ชาย (`e841307`)
กฎ "ผม/ครับ เสมอ ไม่ตามคำลงท้ายในต้นฉบับ" อยู่ใน `head` ของ `posts.script` (`config/aiPrompts.js`) · สโมคผ่าน
**multi-tenant ไม่ต้องห่วง** — แต่ละ org แก้ prompt ทับเองได้ที่ `org_ai_prompts` (`web/db/orgAiPrompts.js`) ไม่ต้องแตะโค้ด

## ⛔ ข้อตกลงสายงานคลิป (ห้ามเปลี่ยนเอง)
- ห้ามให้ AI แต่งเนื้อหาใหม่ในบทพูด — แปลง*รูปแบบ*จาก body เท่านั้น
- **ห้ามเสนอ TTS / avatar / เจนภาพประกอบ** · ภาพประกอบต้องมาจากคลังรูปจริงของ user
- user **ไม่เข้าใจศัพท์เทคนิคเรื่องคลิป** → อธิบายด้วยภาษาคน
- เกณฑ์ขึ้นขั้นถัดไป: ทำคลิปจริง 3-5 คลิปก่อน แล้วดูว่าอะไรน่ารำคาญที่สุด — ไม่ใช่เดาล่วงหน้า

---

## ✅ ก่อนหน้า: KANBAN ชั้น teamspace — ขึ้น prod แล้ว
**เหลือ:** `ALTER TABLE kanban_boards ALTER COLUMN teamspace_id SET NOT NULL;` (เขียนเป็น migration ไว้แล้ว
**ยังไม่ได้รันบน prod** — deploy รอบ 2026-09-08 ตั้งใจไม่รัน `migrate up` เพราะรอบนั้นไม่ต้องใช้)
**⛔ ข้อตกลง:** "teamspace" ทับศัพท์ · custom field ผูกกระดาน ไม่ใช่ teamspace · ยังไม่มีด่านสิทธิ์โดยตั้งใจ —
กันจริงต้องแก้ `web/db/kanban/scopeSql.js` 3 จุดพร้อมกัน · ห้ามเพิ่ม `is_guild_default`/`kanban_board_channels`/`kanban_columns`

## ➜ ค้างไว้ (ไม่ด่วน)
1. Posts โหมด AI "ร่างตามคำแนะนำ" (`5c73c8b`) — user ยังไม่กดทดสอบ
2. แท็บ "นำเข้าแล้ว" ยังไม่มีลิงก์ไปการ์ด KB-xxx · ยังไม่มีปุ่ม "ยกเลิกการนำเข้า"
3. `caseUploads.getCaseUploadDir()` / `cropDocument.getUploadPath()` มีบั๊ก cwd แบบที่แก้ไปแล้ว
4. หนี้ i18n ทั้งโซน posts (`PostEditor.jsx`) — ดู `md/PENDING.md`
5. **ย้าย storage สื่อออกจากดิสก์เว็บ** — จดไว้ใน `md/PENDING.md` แล้ว (ฝั่งบอทอ่านดิสก์ก้อนเดียวกัน ไม่ใช่แก้จุดเดียว)

## ⚠️ รู้ไว้
- **หลังรีบูตเครื่อง dev → ssh เข้า prod จาก session พัง** (`agent refused operation`) เพราะ gnome-keyring ล็อก
  ให้ user รัน `ssh tee@202.183.141.78 true` ในเทอร์มินัลตัวเองครั้งเดียวเพื่อปลดล็อก แล้วค่อยทำต่อ
- **GitHub token ย้ายออกจาก `.git/config` แล้ว** ไปอยู่ `~/.git-credentials` (600) + `credential.helper store`
  **prod ยังมี token ฝังใน remote url** — วันไหนออก token ใหม่ต้องไปแก้ที่ prod ด้วย ไม่งั้น pull ไม่ผ่าน
- **กล้องในเบราว์เซอร์ (`getUserMedia`) ทำงานเฉพาะ https** — เปิดผ่าน `http://<ip>:3000` บนมือถือ กล้องไม่ขึ้น
- dev server ของ user รันที่พอร์ต 3000 — จะ build ต้องตั้ง `NEXT_DIST_DIR` แยก ไม่งั้น dev server 500 ทั้งเว็บ
- ล็อกอินทดสอบ local: insert token ลง `org_login_tokens` ตรงๆ · curl ใช้ไม่ได้ ต้องผ่านเบราว์เซอร์จริง
