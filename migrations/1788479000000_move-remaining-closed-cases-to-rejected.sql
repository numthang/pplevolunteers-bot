-- Up Migration
-- ปิดฉาก `closed` ให้จบ — ย้ายเคสที่เหลือทั้งหมดไป `rejected`
--
-- ต่อจาก 1788470000000 ที่ย้ายเฉพาะ closed+"แก้ไขสำเร็จ" → resolved (65 ใบ)
-- รอบนั้นจงใจเว้นใบที่เหตุผลเป็นการปฏิเสธไว้ เพราะกลัวว่าการเปลี่ยนป้ายสถานะย้อนหลัง
-- จะทำให้ผู้ร้องงงโดยไม่มีคำอธิบาย — **ตรวจแล้วพบว่ากังวลเกินไป**: ทั้ง 15 ใบมีข้อความ
-- ในไทม์ไลน์สาธารณะอธิบายเหตุผลอยู่แล้วตั้งแต่ตอนปิดเรื่อง (has_public_note ครบทุกใบ)
-- และตอนนี้หน้า /complaint/[ref] ก็แสดง close_reason คู่กับป้ายสถานะแล้ว → อ่านแล้วสอดคล้องกัน
-- user เคาะ 2026-09-04: "ไม่ต้องกังวลเรื่องผู้ร้อง ทำข้อมูลให้ถูก"
--
-- close_reason ที่เหลือ (นอกเหนืออำนาจ / ข้อมูลไม่พอ) เป็นเหตุผลของ rejected อยู่แล้ว → เก็บไว้ตามเดิม
-- ต่างจากรอบก่อนที่ล้างเป็น NULL เพราะ "แก้ไขสำเร็จ" ซ้ำซ้อนกับตัวสถานะ resolved
--
-- ⚠️ ห้ามเขียน updated_at = now() — cases ไม่มี trigger, ค่าจึงคงเดิม
--    ถ้าแตะ การ์ด "เสร็จใน 30 วัน" หน้าแรก (countCaseStats) จะเด้งขึ้นทั้งก้อนโดยไม่มีอะไรเกิดขึ้นจริง
--
-- ✅ kanban ไม่ขยับ: closed และ rejected แม็ปเป็น 'done' เหมือนกัน (statusSql.js)
--    และทั้งคู่อยู่ใน KANBAN_DONE_STATUSES → completed_at ไม่ถูกแตะ
-- ✅ หลังรัน: ไม่เหลือแถว closed อีก · เก็บ label/การอ่านไว้ทุกจุดเผื่อข้อมูลเก่าหลุดมา
UPDATE cases
   SET status = 'rejected'
 WHERE status = 'closed';

-- Down Migration
-- ⛔ ย้อนกลับไม่ได้ — แยกไม่ออกว่า rejected ใบไหนเดิมเป็น closed ใบไหนเป็น rejected อยู่แล้ว
-- ถ้าต้องย้อนจริงให้ใช้ audit_logs (action='case.status_changed') ประกอบ แล้วเขียน migration ใหม่
SELECT 1;
