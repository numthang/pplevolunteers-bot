# Kanban — จุดรับช่วง session หน้า (เขียนใหม่ 2026-08-19 เช้ามืด)

> อ่านไฟล์นี้แล้วทำต่อได้เลย ไม่ต้องไล่ถามซ้ำ
> เสร็จเรื่องไหนให้ลบหัวข้อนั้นทิ้ง · เคลียร์หมดแล้วลบไฟล์นี้ได้

## สถานะ

**push ไปแล้วถึง `21af53b` · หลังจากนั้นมีอีก 7 commit ที่ยังไม่ push** (`4989f85` … `3489a13`)

```
3489a13 ถังขยะตัวเลือก/งานย่อย → กล่องแบบ "ลบการบ้าน" (ซ่อน/ลบถาวร)
8139b19 เช็คลิสต์ใช้กติกาเดียวกับ select (rename ในคลัง + ซ่อน/เอากลับ)
1520670 ลบตัวเลือกออกจากคลังเช็คลิสต์ได้  ← ถูกกลืนโดย 3489a13 แล้ว
f3e1c0b ถอด truncate ออกจากชิป
0b3ea29 ชื่อคนโชว์ display_name แทน username ดิบ
237434b chore(wolf) รวม cerebrum เหลือ 4 หัวข้อ
4989f85 ยุบ pending-2026-08-19.sql กลับเข้า migration.sql
```

**ก่อนหน้านั้น 20 commit** (`bc44bf5` … `253ea71`) — push แล้ว แต่ **ยังไม่ deploy**

| กลุ่ม | commit |
|---|---|
| ลบถาวรแบบ Notion · duplicate card · checklist ดึงจากคลัง | `bc44bf5` `0f67bd9` `fdadd17` `a61b22c` |
| กล่องลบทรง posts (เข้ากรุ/ลบถาวร/ยกเลิก) | `2fccb12` |
| รื้อ UI ทั้ง modal เป็นแถว `label \| ค่า` แบบ Notion | `b9a2f07` `fb76771` `d21ce16` |
| combobox เดียวใช้ทั้ง custom field + สถานะ/เจ้าภาพ/คนช่วย | `d21ce16` |
| ลาก field (เงา + เส้นบอกจุดวาง) | `72ce849` `2460604` `cdeb9ca` |
| ก้อน A ใน modal · ก้อน B กระดาน | `7d74006` `d923ed4` |
| แก้ i18n / บั๊ก | `2fdfc07` `253ea71` |

`npm test` 419 ผ่าน · build ผ่าน · **smoke DB เขียนใหม่ 4 ชุด ผ่านหมด**

⚠️ **user กดจริงแล้วเฉพาะก้อน A** — ก้อน B (กระดาน) กับเส้นลาก field **ยังไม่เคยกด**

---

## 🚀 Deploy — มี runbook แยกแล้ว

**[`md/kanban/DEPLOY-2026-08-19.md`](DEPLOY-2026-08-19.md)** — 8 ขั้น ครบตั้งแต่ push ถึงตรวจงาน + วิธีย้อน
user เคาะแล้วว่าการ์ดบน prod ทิ้งได้ → **ไม่ต้องรัน `kanbanLabelsToFields.mjs` บน prod**
DDL ที่ค้างอยู่หลัง marker ใน `scripts/migration/migration.sql` ตามปกติ — ห่อ `BEGIN/COMMIT` ไว้ให้แล้ว
(ซ้อมบนฐานจำลองผ่าน 2 รอบ รันซ้ำได้)

## ⚠️ กับดักตอน deploy

1. **migration ค้าง 21 statement** หลัง marker `-- production ทำถึงตรงนี้`
   (user ขยับ marker ลง 1 ขั้นแล้ว = รัน orgchart บน prod ไปแล้ว)
2. **`ALTER TABLE kanban_card_checklist ALTER COLUMN field_id SET NOT NULL` พังถ้า prod มีข้อมูลเช็คลิสต์**
   เช็คก่อน: `SELECT count(*) FROM kanban_card_checklist;` (dev มี 0 แถวตอนแปลง)
3. **บอทกับเว็บต้องขึ้นพร้อมกัน** — `db/kanbanCards.js` เขียน `source_url`/`source_message_id`
4. ⛔ **ลำดับบน prod ห้ามสลับ** (dev ทำครบแล้ว · migration.sql เขียนกำกับไว้ท้ายไฟล์)
   1) รัน `scripts/migration/kanbanLabelsToFields.mjs` dry-run → 2) `--commit` ย้ายข้อมูล
   3) deploy โค้ดใหม่ → 4) ตรวจว่าแท็กขึ้นครบ → 5) ค่อย `DROP TABLE kanban_card_labels, kanban_labels`
   ขึ้นโค้ดก่อนย้าย = แท็กหายต่อหน้า · ลบตารางก่อนย้าย = กู้ไม่ได้
5. **`blocked`/`blocked_reason` ถูก DROP แล้ว** — เช็คก่อนบน prod: `SELECT count(*) FROM kanban_cards WHERE blocked;` (dev ได้ 0)
6. ⛔ **ห้ามเชื่อว่า prod อยู่สถานะไหนจากไฟล์นี้ — ถาม user เสมอ**

---

## งานที่เหลือ

**เริ่มตรงนี้:** ข้อ 7 (ให้ user กดเทสของคืน 19 ส.ค.) → ข้อ 1b (เติมชื่อคน) → deploy
ข้อที่ขึ้น ✅ คือบันทึกไว้กันรื้อซ้ำ ไม่ต้องทำอะไร


### 1. 🟡 ก้อน B + เส้นลาก field — user บอก "ลองเล่นบ้างแล้ว" แต่ยังไม่ยืนยันรายข้อ

กระดาน: ปุ่ม **+ เพิ่มการ์ดในกอง** · เมนู **...** บนการ์ด · **ปากกา** แก้ชื่อในที่
ลาก field ในกล่อง "ข้อมูลของทีม" → เส้นบอกจุดวางขึ้นเส้นเดียวตรงที่เมาส์อยู่
เจอบั๊กแล้วแก้ไป 2 ตัว (การ์ดใหม่เด้งลงล่าง · กล่องลบมีแต่ปุ่มเข้ากรุ) — ที่เหลือยังไม่มีรายงาน

### 1b. 🔴 5 ชื่อใน `scripts/import/people-map.json` ยังจับคู่ไม่ได้

`Ji · Nuu · Oat · Ploy · Whoon` → **3 ใบไม่มีเจ้าภาพ** (Design Week / One day trip / อบรมการคิดเชิงนโยบาย)
\+ **7 ใบคนช่วยหาย** · ไฟล์นี้ gitignore ต้องเติมบนเครื่องที่รัน import
`"Mek": null` เป็นเคสเดียวกัน — แก้แล้ว (= 2353 `@undermek` "เมฆ") + ซ่อมการ์ด 7 ใบใน DB ตรงๆ

⚠️ **บั๊กเชิงดีไซน์ของสคริปต์ที่ยังไม่ได้แก้:** ชื่อที่จับคู่ไม่ได้ถูก `.filter(Boolean)` ทิ้ง
แล้ว **เลื่อนคนถัดไปขึ้นเป็นเจ้าภาพ** — `"Mek, Mark"` เลยกลายเป็น Mark เป็นเจ้าภาพ
ควรทำให้การ์ด "ไม่มีเจ้าภาพ" แทน (เห็นชัดว่าต้องแก้) ไม่ใช่เจ้าภาพผิดคนที่ดูเหมือนถูกทุกประการ

### 1c. 🟡 คลัง `อุปกรณ์` มีตัวไม่มีใครใช้ 2 ตัว

`ลำโพง+ไมโครโฟน` (สร้าง 19 ส.ค. 23:05 — ขยะจากบั๊ก fork ที่แก้ไปแล้ว) · `ปากกา` (00:46 — อาจตั้งใจ)
ไม่ได้ลบให้เพราะแยกไม่ออกว่าอันไหนตั้งใจ · ตอนนี้ user ลบเองได้จาก UI แล้ว
มีคู่คล้ายจาก import ด้วย: `ปริ้นเตอร์`/`พรินเตอร์` · `แนบท้าย3`/`แบบท้าย3`

### 2. ✅ ยุบป้าย → custom field — **เสร็จแล้ว 2026-08-19**
ข้อมูลย้ายครบ **ตกหล่น 0** (พื้นที่ 13 · สายงาน 57 · อุปกรณ์ 16 เส้น) · ตารางป้ายยังอยู่ครบ ยังไม่ DROP
สคริปต์: `node --env-file=.env scripts/migration/kanbanLabelsToFields.mjs [--commit]` — dry-run ก่อนเสมอ · รันซ้ำได้
ทางเขียนแท็กจุดเดียว = `web/db/kanban/tags.js` · ทางแปลง field→ชิปจุดเดียว = `cardTags()` ใน `lib/kanbanTagFilter.js`
⛔ **prod ไม่มี field พวกนี้** (user สร้างเองผ่าน UI บน dev) — สคริปต์สร้างให้เองจาก **ชื่อ** ห้ามอ้าง id/key

### 3. ✅ import 82 ใบจาก xlsx — **เสร็จแล้ว 2026-08-19**
`TRUNCATE kanban_cards RESTART IDENTITY CASCADE` แล้ว import ใหม่ทั้งชุด (user เคาะ: ล้างแล้วเอาเข้าใหม่หมด)
→ **82 ใบ K-1..K-82 · แท็ก 288 · งบประมาณ 13 · คนช่วย 72 · error 0**
สำรองก่อนล้าง: `backups/kanban/pre-truncate-20260819-1010.sql`

**คอลัมน์ xlsx → ปลายทาง (11/12 เข้าระบบ):**
Title→title · Description→detail · ผู้รับผิดชอบ→เจ้าภาพ+คนช่วย · Date→start/due · Status→status_type
category→field สายงาน · อำเภอ→field พื้นที่ · อุปกรณ์→field อุปกรณ์ (checklist) · งบประมาณ→field งบประมาณ (number)
**Discord→`kanban_cards.source_url`** (ช่องประจำ ไม่ใช่ custom field — บอทใช้ช่องเดียวกัน)
⛔ **FB Post ไม่เอาเข้า** (user เคาะ) — ลิงก์โพสต์อยู่โมดูล posts แล้ว ทำซ้ำ = 2 ที่ไม่ตรงกัน
❌ **Checklist กู้ไม่ได้ถาวร** — AppFlowy export มาเป็น % (`0.73`) ไม่มีตัวข้อความ

⚠️ **งบประมาณในไฟล์มีแค่ 13/82** — user ยังไม่ได้เติมตอน import · เติมเพิ่มได้ 2 ทาง:
กรอกในเว็บทีละใบ **หรือ** เติม xlsx แล้ว truncate+import ใหม่ (จะทับของที่กรอกในเว็บไปแล้ว)

### 4. ✅ ติดตั้ง ESLint — **เสร็จแล้ว 2026-08-19**
`npm run lint:all` ที่ root = ตรวจทั้งบอทและเว็บ · baseline **0 error** (web 97 warn / bot 42 warn)
เปิดเฉพาะ rule ที่จับบั๊กจริง (`no-undef` = error) rule สไตล์ปิดหมด — **ห้ามเปิดเพิ่มโดยไม่ถาม**
รันครั้งแรกเจอบั๊กจริง 2 ตัวที่ build + test 419 ข้อจับไม่ได้ (bug-427, bug-428)
→ **หลังลบ/ย้ายฟังก์ชันทุกครั้ง ให้รัน `npm run lint:all` ไม่ใช่แค่ build**

### 5. ✅ ชื่อคนที่โชว์ + ป้ายชื่อตกบรรทัด — **เสร็จแล้ว 2026-08-19 (รอบบ่าย)**
**สูตรชื่อย้ายมาอยู่ที่ `web/db/displayName.js` ที่เดียว** (เดิมก็อปใน `db/kanban/cards.js` + `db/kanban/people.js`
พร้อมคอมเมนต์เตือนกันเองว่าอย่าให้ต่างกัน = รอวันดริฟต์)
ลำดับใหม่: **org display_name → nickname → ชื่อจริง → username**
เดิมเริ่มที่ชื่อจริงซึ่งมีแค่ 1,402/7,490 คน (19%) → 81% ตกไปโชว์ username ดิบ (`mark30260`, `chayanankoj`)
- ⚠️ คนเดียวมีหลายแถวใน `org_members` (org 1 คร่อม 3 guild · 708 คนซ้ำ) → subquery ต้อง `ORDER BY id LIMIT 1`
- ต้องมี index `idx_org_members_user_org` (อยู่ใต้ marker ใน migration.sql แล้ว) ไม่งั้น seq scan ต่อแถว
- กล่องเลือกคนโชว์ `@username` เป็นบรรทัดรอง — org นี้มี "Ploy" 6 คน · **ห้ามต่อเข้าไปในชื่อ** ไม่งั้นชิปติด `(@…)` ไปด้วย
- ลบ `OwnerPicker.jsx` ทิ้ง (ไม่มีใคร import ตั้งแต่ย้ายไปใช้ TagCombobox)
- ชิปที่มีเว้นวรรคเคยตัดบรรทัดกลางชิป → `whitespace-nowrap` + `truncate` ทั้ง 3 จุด (TagCombobox / LabelChips / ตัวกรอง)

**🔴 ยังค้าง — 5 ชื่อใน `scripts/import/people-map.json` ยังจับคู่ไม่ได้:** `Ji · Nuu · Oat · Ploy · Whoon`
ผลคือ **3 ใบไม่มีเจ้าภาพ** (Design Week / One day trip / อบรมการคิดเชิงนโยบาย) + **7 ใบคนช่วยหาย**
`"Mek": null` ก็เป็นเคสนี้ — แก้แล้ว (= 2353 `@undermek` "เมฆ") + ซ่อมการ์ด 7 ใบใน DB ตรงๆ (16/27/38/40/44/54/61)
→ ให้ user บอกว่าใครเป็นใคร แล้วเติมลง map (ไฟล์นี้ gitignore — ต้องเติมบนเครื่องที่รัน import)

### 6. ✅ ตัวเลือกในคลัง — กติกาเดียวทั้ง select / multi_select / checklist (**เสร็จ 2026-08-19 ค่ำ–ดึก**)

**กลับคำ 3 รอบในวันเดียว อ่านเหตุผลก่อนแตะ ไม่งั้นจะรื้อกลับไปที่เดิม:**

| รอบ | ทำอะไร | ทำไมถึงพัง |
|---|---|---|
| เช้า | แก้ข้อความงานย่อย = "เฉพาะการ์ดใบนี้" ด้วย `ensureFieldOption(ชื่อใหม่)` | คลังงอกตัวใหม่ทุกครั้งที่แก้ ตัวเก่าเป็นขยะกำพร้า |
| ค่ำ | user: *"เอาเหมือน select เลย พฤติกรรม"* | — |
| ดึก | user + สกรีนช็อต: ถังขยะทุกที่ต้องขึ้นกล่องแบบ "ลบการบ้าน" | `window.confirm` ให้เลือกได้แค่ ตกลง/ยกเลิก |

**กติกาที่นิ่งแล้ว:**

| การกระทำ | ผล (เหมือนกันทั้ง 3 ชนิด) |
|---|---|
| แก้ชื่อ | `updateFieldOption()` rename ในคลัง → **ทุกการ์ดตาม** · ชื่อซ้ำ = 409 เด้งค่าเดิมกลับ |
| **ซ่อน** | archive ตัวเลือก **+ เอาออกจากการ์ดใบนี้ พร้อมกัน** · การ์ดใบอื่นยังเห็น · เอากลับได้ |
| **ลบถาวร** | ลบจากคลัง แถวเช็คลิสต์ในทุกการ์ดถูก DELETE ด้วย · กู้ไม่ได้ |

- ถังขยะทุกที่เปิด **`DeleteChoiceDialog`** (กล่องเดียวกับลบการ์ด) — **ไม่มี `window.confirm` เหลือในโมดูลนี้แล้ว**
- **เช็คลิสต์ไม่มีเมนู `...`** (user: *"ไม่ต้องมีไข่ปลา เปลี่ยนสี/delete tag เอาออกไปเลย ไม่ได้ใช้"*)
  ชิปคลัง = กดหยิบอย่างเดียว · จัดการตัวเลือกทำจากถังขยะท้ายงานย่อยที่เดียว · เอากลับจากกอง "ซ่อนไว้ (N)"
- select/multi_select ยังมี `...` เพราะต้องเปลี่ยนสีชิป — แต่ปุ่ม "ซ่อน" ในนั้นถูกถอด (ซ้ำกับกล่อง)
- **ทางเขียนเดียว:** `web/lib/kanbanOptionActions.js` + `OptionEditor` export จาก `TagCombobox.jsx`
  ⛔ ห้ามยิง `/api/kanban/fields/../options` เองในคอมโพเนนต์ ห้ามลอก markup ไปวางซ้ำ

⛔ **กับดักที่ฆ่า archive ไปแล้วรอบหนึ่ง (2026-08-18) — ห้ามทำซ้ำ:**
`cards.js` เคยมี `AND o.archived_at IS NULL` ใน JOIN ที่ดึงชิปของการ์ด → ซ่อนแล้วชิปหายเกลี้ยงทุกการ์ด
= ไม่ต่างจากลบ เลยสรุปผิดว่า "archive ไม่มีประโยชน์" แล้วถอดทิ้ง
**ต้นเหตุอยู่ที่เงื่อนไขนั้น ไม่ใช่ที่ archive** · ถอดออกแล้ว — ใส่กลับเมื่อไหร่ ฟีเจอร์ตายทันที

**ไม่มี migration** — ใช้คอลัมน์ `archived_at` ที่มีอยู่แล้ว

### 7. 🔴 ยังไม่ได้กดจริง — ของที่ทำคืน 19 ส.ค.

user ยังไม่ได้กดเทสหลังรอบสุดท้าย ให้ส่งไปกดก่อนทำอย่างอื่น:
- ถังขยะท้ายงานย่อย → กล่องขึ้นไหม · **ซ่อน** แล้วหายจากการ์ดใบนี้ + ไปโผล่ในกอง "ซ่อนไว้" ไหม
- **เอากลับ** แล้วชื่อกลับเข้ารายการแนะนำไหม
- **ลบถาวร** แล้วหายจากการ์ดใบอื่นด้วยไหม (เปิดอีกใบเช็ค)
- แก้ข้อความงานย่อย → การ์ดใบอื่นที่ใช้ชื่อเดียวกันเปลี่ยนตามไหม · ตั้งชื่อซ้ำแล้วเด้งกลับไหม
- ถังขยะของ select/multi_select (ในเมนู `...`) → กล่องเดียวกันไหม

---

## เอกสารที่ต้องอ่านก่อนแตะโค้ด

- `md/kanban/KANBAN.md` §ลบถาวร · §ทำสำเนาการ์ด · §"พักไว้" กับ "กรุ" (ห้ามยุบเข้าหากัน)
- `md/kanban/CUSTOM-FIELDS.md` §กลับคำรอบเย็น
- `.wolf/cerebrum.md` §Do-Not-Repeat 2026-08-18/19
- `web/components/kanban/FieldRow.jsx` — **ของระบบกับ custom field ใช้แถวตัวนี้ตัวเดียวกัน ห้ามเขียน grid ซ้ำ**
- `web/components/kanban/TagCombobox.jsx` — 3 โหมด (`field`/`static`/`search`) · โหมด field คือเส้นเดิม **ห้ามแตะ**
- `web/db/kanban/tags.js` — **ทางเขียนแท็กจุดเดียว** (สคริปต์ import + สคริปต์ย้ายข้อมูล ใช้ตัวนี้ร่วมกัน)
- `web/lib/kanbanTagFilter.js` §`cardTags()` — **ทางแปลง field → ชิปจุดเดียว** ห้ามเขียนซ้ำที่อื่น
- `web/lib/kanbanOptionActions.js` — **ทางแตะตัวเลือกในคลังจุดเดียว** ห้ามยิง API เองในคอมโพเนนต์
- `web/db/displayName.js` — **สูตรชื่อคนของทั้งระบบจุดเดียว** (org display_name → nickname → ชื่อจริง → username)
- `web/components/kanban/DeleteChoiceDialog.jsx` — **ถังขยะทุกที่ต้องขึ้นกล่องนี้** ห้ามใช้ `window.confirm`

---

## นอกโมดูล — ของที่จดไว้ที่อื่น

- **การ์ดโปรไฟล์คนแบบ Discord (กล่องลอย + ส่ง DM ผ่านบอท)** → `md/PENDING.md` §👤
  user สั่ง 2026-08-19 ว่า "เก็บไว้ก่อน ทำ session หน้า" · ยังไม่มีหน้าโปรไฟล์คนในระบบเลย
- **board จริง** — `kanban_field_defs` มี `board_id` แล้ว แต่ `kanban_cards` **ยังไม่มีคอลัมน์ board_id ด้วยซ้ำ**
  `cards.js` ฮาร์ดโค้ด `d.board_id IS NULL` = ตอนนี้ field เป็นของกลางทั้ง org ทั้งหมด (งานก้อน 3)
  user เคาะแล้วว่าคลังตัวเลือก **ห้ามข้าม board** — โครงสร้างรองรับอยู่แล้ว (option ผูก field · field ผูก board)
